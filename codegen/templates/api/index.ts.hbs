{{#resources}}
import {{this.id}} from './{{this.id}}';
{{/resources}}
import Auth from './Auth';
import Tokens from './Tokens';

export {
    {{#resources}}
    {{#if (isSubscriptionIntegration this.id) }}{{this.id}} as SubscriptionEvent{{else}}{{this.id}}{{/if}},
    {{/resources}}
    Auth,
    Tokens
}